{% extends "base.html.twig" %}

{% block title %}Card Deck{% endblock %}

{% block main %}
    <h1>Card Deck</h1>

    <ul>
    {% for card in deck_of_cards %}
        {% if card is same as('🂱') %}
        <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂲') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂳') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂴') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂵') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂶') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂷') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂸') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂹') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂺') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂼') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂽') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂾') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃁') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃂') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃃') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃄') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃅') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃆') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃇') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃈') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃉') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃊') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃌') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃍') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃎') %}
            <li class="red-card">{{ card }}</li>
        {% else %}
            <li class="black-card">{{ card }}</li>
        {% endif %}
    {% endfor %}
    </ul>

{% endblock %}
{% extends "base.html.twig" %}

{% block title %}Card Deck Draw{% endblock %}

{% block main %}
    <h1>Card Deck Draw</h1>
    <div>
        {% if card is same as('🂱') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂲') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂳') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂴') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂵') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂶') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂷') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂸') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂹') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂺') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂼') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂽') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🂾') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃁') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃂') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃃') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃄') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃅') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃆') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃇') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃈') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃉') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃊') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃌') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃍') %}
            <li class="red-card">{{ card }}</li>
        {% elseif card is same as('🃎') %}
            <li class="red-card">{{ card }}</li>
        {% else %}
            <li class="black-card">{{ card }}</li>
        {% endif %}
    </div>

    <div>
        <p>Cards left in deck: {{ cards_left }}</p>
    </div>
{% endblock %}
